# 🔄 ملف التسليم — اقرأه قبل ما تبدأ

> **للمساعد الجاي:** الملف ده بيقول لك إيه اللي اتعمل، وإيه اللي لسه، وإيه
> القرارات اللي اتاخدت وليه. **اقراه كله قبل ما تلمس أي حاجة** — فيه ٣ حاجات
> مقفولة عن قصد ولو فتحتها هتكسر البرنامج.

**آخر تحديث:** ٢٠ أغسطس ٢٠٢٦
**الفرع:** `main` (مرفوع على `github.com/zpzz300-rgb/baqat`)

---

## 👤 مين المستخدم

تاجر خطوط مصري — **مش مبرمج**. بيتكلم عربي عامية.

**قواعد لازم تمشي عليها:**

1. **اشرح بالعامية قبل ما تكتب كود.** لو الطلب فيه أكتر من قراية، اعرض
   الخطة واستنّى الموافقة. السهولة أهم من كتر المميزات.
2. **عمرك ما تعمل `flutter install` على التليفون** — بتمسح البرنامج الأول
   فالبيانات الحيّة بتضيع. استخدم `adb install -r`.
3. **البرنامج شغّال في شغل حقيقي** بفلوس وعملاء. أي تغيير في الحسابات لازم
   يتغطّى باختبار الأول.
4. **التقارير معلومات مش أسئلة** — كمّل ما تقفش تستأذن كل خطوة.

---

## 🏗 البرنامج

Flutter + Provider + Supabase. تاجر بيدير خطوط محمول ويأجّرها لعملاء.

```
lib/models/models.dart          كل الموديلات + الحسابات (المصدر الوحيد للمنطق)
lib/providers/app_provider.dart الحالة + الحفظ + المزامنة (~4500 سطر)
lib/screens/                    الشاشات
lib/services/breakpoints.dart   مقاسات الشاشات — مصدر واحد
lib/services/responsive.dart    أدوات التجاوب المشتركة
test/                           ١٨٧ اختبار
```

### 📐 قاعدة معمارية مهمة

**أي منطق حسابي لازم يبقى في `models.dart` مش في `app_provider.dart`.**

السبب: البروڤايدر محتاج Supabase عشان يشتغل، فالاختبارات مابتوصلش له. لما
حطّينا منطق توزيع دفعة الكفيل في البروڤايدر، القاعدة اللي بتقول «اللي
ماعليهوش دين مابياخدش» ماكانش حد بيتأكد منها — وطلعت غلط في الشغل الحقيقي.

النمط الصح:

```dart
// models.dart — الحساب، متغطّى باختبارات
({Map<String, double> shares, double leftover}) splitGuarantorPayment(...)

// app_provider.dart — بينده على الموديل وبيحفظ ويسجّل
int payGuarantor(...) { final r = db.splitGuarantorPayment(...); ... }
```

---

## ✅ اللي خلص

### مراجعة شاملة — ٤٩ من ٥٠ بند

| القسم | |
|---|---|
| (أ) إصلاح الأخطاء | ٨/٨ |
| (ب) الحماية | ٨/٨ |
| (ج) دورة «شهر وشهر» | ٨/٨ |
| (د) الفلوس | ٨/٨ |
| (هـ) التصميم | ٩/١٠ |
| (و) الاستخدام اليومي | ٨/٨ |

**الاختبارات: ٥٥ ← ١٨٧**

### أهم الأنظمة اللي اتبنت

**🔁 دورة «شهر وشهر»** — الخط ليه حاجتين بس: **رقم** (الفاتورة بكام)
و**علامة** (آخر فاتورة نزلت شهر كام). منهم البرنامج يحسب أي شهر — ماضي أو
مستقبل — الخط ده دوره ولا لأ.

```dart
// models.dart — Group.isDueIn()
final n = billCycleMonths;              // ٢ افتراضي، بيدعم ٣ و٤
return ((gap % n) + n) % n == 0;        // تصحيح السالب مقصود
```

⚠️ **`((gap % n) + n) % n` مش `gap % n`** — عشان الحساب يمشي **ورا** كمان.
شهر قبل المرساة بيدّي باقي بالسالب والمقارنة بصفر بتفشل، فالتاريخ القديم
كله يطلع غلط. فيه اختبار مخصوص للحالة دي.

**💰 فصل «فاتورة الشركة» عن «أساس الربح»** — رقمين مختلفين تماماً:
- `fixedBillAmount` = اللي الشركة بتاخده (٤٢٥٠)
- `profitBillAmount` = تكلفة الشهر الواحد اللي الربح بيتحسب عليها (٢١٢٥)
- `profitBasis => profitBillAmount ?? fixedBillAmount`

الأسامي كلها من `lib/widgets/money_words.dart` — **مصدر واحد**. أي شاشة
جديدة تعرض فلوس تاخد الاسم من هناك، ما تخترعش اسم.

**🤝 توزيع دفعة الكفيل** — [شوف قسم «آخر شغل» تحت]

---

## 🚧 الشغل اللي واقف دلوقتي — الكفيل

**ده آخر حاجة كنت شغّال عليها. الحساب والبروڤايدر خلصوا، الشاشة نصّها.**

### المشكلة اللي المستخدم اشتكى منها

> «بدفع للكفيل، أدخل السجل مالاقيش حاجة. والحساب اتلخبط.»

### الأسباب الخمسة (كلها اتأكدت من الكود)

**١) دالتين دفع منفصلتين.** `guarantorBulkPay` كانت بتوزّع على العملاء
**من غير ما تكتب في سجل الكفيل**، و`recordGuarantorPayment` بتكتب في السجل.
فتدفع من كارت الكفيل وتفتح السجل تلاقيه فاضي.

**٢) «توزيع متساوٍ» كان بيدّي فلوس لناس ماعليهاش حاجة.** ده كان الافتراضي.
بأرقام المستخدم الحقيقية:

```
٣ خطوط: +٦٠ (مسدّد) · −٤٩٠ · −٢٠٩٠     الدين = ٢٥٨٠
دفع ١٦٥٠ مرتين = ٣٣٠٠
التوزيع المتساوي: ٥٥٠ لكل واحد كل مرة
→ الدين نزل ١٥٩٠ بس، و١٧١٠ اتحوّلوا رصيد لناس ماعليهاش حاجة
```

**٣) «حسب المديونية» اسمها كان بيكدب.** الكود كان `totalAmount /
debtors.length` — يعني **قسمة بالتساوي على المدينين**، مش بالتناسب مع دين
كل واحد. فاللي عليه ٤٦٠ واللي عليه ٣٧٠٠ ياخدوا نفس الرقم.

**٤) مفيش سقف.** محدش كان بيتحدّ بدينه، فالزيادة بتتحوّل رصيد بدل ما تفضل
عند الكفيل.

**٥) المسح ماكانش بيرجّع فلوس.** `deleteGuarantorLog` كانت بتشيل سطر السجل
بس — الفلوس تفضل متخصومة من العملاء للأبد. **دي كانت أخطرهم** لأنها بتسيب
البيانات غلط من غير ما حد ياخد باله.

### ✅ اللي اتعمل

```
models.dart          splitGuarantorPayment()      ← الحساب، ١٠ اختبارات
app_provider.dart    payGuarantor()               ← دفع + سجل + batchId
app_provider.dart    undoGuarantorPayment()       ← تراجع حقيقي
app_provider.dart    legacyGuarantorPayments()    ← الدفعات القديمة
guarantors_screen    القايمة المنسدلة + المعاينة + زرار التأكيد
test/guarantor_pay_test.dart                      ← ١٠ اختبارات
```

**الأوضاع الجديدة:**
- `proportional` (افتراضي) — كل واحد ينزل بنفس نسبة دينه
- `oldestFirst` — يخلّص الأكبر ديناً الأول
- `guarantorOnly` — على الكفيل بس من غير توزيع

**قواعد متغطّية باختبارات:**
- اللي ماعليهوش دين مابياخدش ولا جنيه
- محدش ياخد أكتر من اللي عليه
- الزيادة تفضل عند الكفيل (`leftover`)
- مجموع الحصص = المبلغ بالظبط (آخر واحد بياخد الباقي — عشان كسور القسمة
  ما تتراكمش)

### ❌ اللي لسه

**١) شاشة الدفعات القديمة** — المستخدم اختار: «شاشة توريني وأمسح بإيدي».

`legacyGuarantorPayments()` جاهزة في البروڤايدر وبترجّع الدفعات اللي
مالهاش `batchId` (يعني القديمة اللي مش قابلة للتراجع أوتوماتيك).
**الشاشة نفسها لسه ما اتعملتش.** المطلوب:

```
دفعات الكفيل القديمة:
  ١٦٥٠ ج · ١٧/٨ · موزعة على ٣    [رجّع]
  ١٦٥٠ ج · ١٧/٨ · موزعة على ٣    [رجّع]
```

**٢) `guarantorBulkPay` القديمة لسه موجودة** — محدش بينده عليها دلوقتي، بس
لازم تتشال بعد ما تتأكد إن مفيش حاجة بتستخدمها.

**٣) زرار المسح في السجل لسه بينده على `deleteGuarantorLog`** (اللي
مابترجّعش فلوس) — لازم يتحوّل لـ `undoGuarantorPayment`.
مكانه: `guarantors_screen.dart` — دوّر على `deleteGuarantorLog`.

**٤) المستخدم لسه ما جرّبش النظام الجديد.**

---

## 🚫 تلات حاجات مقفولة عن قصد — ما تفتحهاش

### ١) القايمة الجانبية الثابتة — `hasSideRail = false`

**اتجرّبت تلات مرات وفشلت.** الشكل: القايمة والمحتوى جنب بعض في `Row`.
النتيجة **شاشة سودا** على الشاشة العريضة.

**التحقيق الأخير (بالتشغيل الفعلي على ويندوز والتصوير):**
- المحتوى أسود، **بس فقاعة الملاحظات ظاهرة فوقه**. يعني الـ`Stack` بترسم
  أولادها الطايرين — اللي مش بيترسم هو `Positioned.fill(content)` لوحده.
- **صفر استثناءات وصفر overflow** في الكونسول → المشكلة **رسم مش تخطيط**.
- `test/side_rail_layout_test.dart` بيثبت إن نفس التركيب
  (`Row → Expanded → Stack → IndexedStack → Navigator`) بيتحسب صح لوحده.

**اللي اتجرّب وماحلّش:** `Positioned.fill` على المحتوى · تحويل `Drawer`
لـ`Material` عادي.

**الخيط اللي لسه ما اتتبعش:** ليه أولاد الـ`Stack` الطايرين بيترسموا
والمحتوى لأ؟ ده بيشاور على طبقة رسم (`RepaintBoundary` / `Navigator` جوّه
`IndexedStack`) مش على تخطيط.

### ٢) قايمة الموظفين — ما تحوّلهاش لأعمدة

الـ`ScrollController` بتاعها بيشغّل `DraggableScrollableSheet`. لو قسمتها
أعمدة الشيت هيقف.

### ٣) صفحة «معاينة كل الشاشات» — اتقرر ما تتعملش

القايمة ☰ فيها كل الشاشات بالأقسام وفيها بحث. صفحة تانية بنفس المحتوى =
مكانين للتنقّل والمستخدم ينسى بيستخدم أنهي واحد.

---

## ⚠️ فخاخ اتوقعت فيها — ما تكررهاش

**١) `context.isWide` مش دايماً الصح.** في كارت المجموعة قسمت الكارت
عمودين بناءً على عرض **الشاشة** — بس الشاشة العريضة بتعرض ٣ كروت جنب بعض،
يعني الكارت نفسه ضيّق. النتيجة: ٤٢ خطأ فيض وأرقام مقصوصة في كروت العملاء.
**القياس لازم يبقى على عرض الودجت نفسها (`LayoutBuilder`).**

**٢) الزحلقة الأفقية بتلخبط الهيدر اللي بيتلمّ.** الشاشات مليانة صفوف
شرايح بتتزحلق بالعرض. `CollapsingHeader` بيفلتر
`n.metrics.axis != Axis.vertical` — **ما تشيلش الفلتر ده**.

**٣) `git show HEAD:file` مش هيرجّعلك تعديلات مش متكومتة.** قسمت دالة كبيرة
وحاولت أرجّع الأصل من git فلقيت تعديلاتي راحت. **اعمل كومت قبل أي جراحة
كبيرة.**

**٤) تقسيم دالة `build` كبيرة بيكسر.** جرّبت أقسم `build` في
`company_invoices_screen` عشان ألفّها باختصارات الكيبورد — طلع ١٢ خطأ لأن
الجزء المقسوم بيستخدم متغيرات محلية. **لُف في المكان، ما تقسمش.**

**٥) `PrintWindow` بيرجّع أسود مع Flutter** (بيرسم بكرت الشاشة). للتصوير:
`SetWindowPos` بـ`HWND_TOPMOST` وبعدين `CopyFromScreen`.

---

## 🛠 أوامر الشغل

```bash
flutter analyze lib     # ٦ معلومات قديمة معروفة = نضيف
flutter test            # ١٨٧ لازم كلهم ينجحوا

flutter build windows --release
flutter build apk --release

# المثبّت (ويندوز)
"C:\Users\<user>\AppData\Local\Programs\Inno Setup 6\ISCC.exe" windows\installer.iss
# الناتج: build\windows\installer\تنصيب برنامج الخطوط.exe
# المستخدم بيستلمه في: Desktop\برامج وتثبيت\

adb install -r build\app\outputs\flutter-apk\app-release.apk
```

⚠️ **`.env` مش في git** (فيه مفاتيح Supabase حقيقية). المستخدم عنده نسخة —
اطلبها منه على الجهاز الجديد، أو انسخ `.env.example` واملاه.

---

## 📋 لو هتكمّل — ابدأ من هنا

1. **شاشة الدفعات القديمة** (`legacyGuarantorPayments` جاهزة، الشاشة لأ)
2. **حوّل زرار المسح** لـ`undoGuarantorPayment`
3. **شيل `guarantorBulkPay`** القديمة
4. **استنّى رأي المستخدم** على نظام الكفيل الجديد قبل ما تبني فوقه
